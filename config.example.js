// Copiá este archivo a config.js y completá con los datos de tu proyecto
// de Supabase (Project Settings > API). config.js no se sube a git.
export const SUPABASE_URL = 'https://TU-PROYECTO.supabase.co';
export const SUPABASE_PUBLISHABLE_KEY = 'sb_publishable_XXXXXXXXXXXXXXXXXXXX';

// Dominio de la plataforma para subdominios tipo <slug>.plataformagym.com.ar
// (opcional, se usa como fallback cuando el gimnasio no tiene dominio propio).
export const PLATFORM_DOMAIN = 'plataformagym.com.ar';

// Solo en desarrollo local: qué gimnasio cargar cuando no hay dominio real.
export const DEV_GYM_SLUG = 'forge';
